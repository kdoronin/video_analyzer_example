# Video Analyzer with Automatic Chunking

Продвинутый анализатор видео с автоматическим разбиением на части и анализом через Gemini 2.5 Pro.

## Архитектура

Проект разбит на модули для лучшей организации кода:

- **`send_video_to_gemini.py`** - основной скрипт-координатор
- **`video_processor.py`** - разбиение видео на куски по 10 минут (использует ffmpeg)
- **`gemini_analyzer.py`** - анализ видео через Gemini API
- **`result_combiner.py`** - объединение результатов анализа
- **`file_utils.py`** - утилиты для работы с файлами

## Функциональность

1. **Автоматическое разбиение**: Видео длиннее 10 минут автоматически разбивается на куски через ffmpeg
2. **Быстрая нарезка**: Использует `ffmpeg -c copy` для быстрого разбиения без перекодирования
3. **Контекстный анализ**: Каждый кусок анализируется с учетом его позиции в видео
4. **Объединение результатов**: AI создает единый связный анализ из всех частей
5. **Устойчивость к ошибкам**: Автоматические повторные попытки при превышении лимитов API
6. **Промежуточные файлы**: Сохраняются в папке `temporary/` для отладки
7. **Итоговый результат**: Единый `.txt` файл с полным описанием

## Использование

1. **Установите Google Cloud SDK** (обязательно для работы с Vertex AI):
   
   Перейдите на [официальную страницу установки](https://cloud.google.com/sdk/docs/install) и выберите инструкции для вашей операционной системы:
   
   - **macOS**: Скачайте архив и запустите `./install.sh`
   - **Linux**: Скачайте архив и запустите `./install.sh`  
   - **Windows**: Скачайте установщик GoogleCloudSDKInstaller.exe
   
   После установки выполните аутентификацию:
   ```bash
   gcloud auth application-default login
   ```

2. Убедитесь, что установлен ffmpeg:
```bash
ffmpeg -version
```

3. Установите Python зависимости:
```bash
pip install -r requirements.txt
```

4. Настройте конфигурацию:
```bash
# Скопируйте шаблон конфигурации
cp config.env.example config.env

# Отредактируйте config.env и обязательно измените:
# GOOGLE_CLOUD_PROJECT_ID=your-google-cloud-project-id
# на ваш реальный Google Cloud Project ID
```

⚠️ **ВАЖНО**: Обязательно замените `your-google-cloud-project-id` на ваш реальный Google Cloud Project ID!

5. Поместите ваши видеофайлы в папку `video/` (папка создается автоматически при первом запуске)

6. Запустите анализ всех видео:
```bash
python send_video_to_gemini.py
```

### Пример работы:

```bash
🎬 Found 2 video file(s) to process:
   1. presentation.mp4
   2. meeting_recording.mov

🔄 Processing video 1/2
================================================================================
🎬 Starting analysis for: presentation.mp4
📁 Temporary files will be stored in: temporary
================================================================================
🔪 Step 1: Processing video chunks...
Video duration: 847.50 seconds (14.1 minutes)
Splitting video into 2 chunks of 10.0 minutes each
Creating chunk 1/2: starting at 0.00s
Creating chunk 2/2: starting at 600.00s
Video split into 2 chunks successfully

🤖 Step 2: Analyzing 2 chunk(s) with Gemini...
[... анализ каждого куска ...]

✅ ANALYSIS COMPLETED SUCCESSFULLY for presentation.mp4!
📄 Analysis saved to: presentation.txt

🔄 Processing video 2/2
[... аналогично для meeting_recording.mov ...]

🏁 BATCH PROCESSING COMPLETED!
📊 Summary:
   • Total videos: 2
   • Successfully processed: 2
   • Failed: 0
   • Total processing time: 485.3 seconds (8.1 minutes)

🎉 All videos processed successfully!
```

## Структура проекта

```
project_root/
├── send_video_to_gemini.py    # Основной скрипт
├── config.env                 # Конфигурация (создается из .example)
├── video/                     # Папка для входных видеофайлов
│   ├── README.md              # Инструкции по использованию
│   ├── video1.mp4             # Ваши видеофайлы
│   └── video2.mp4
├── video1.txt                 # Анализ для video1.mp4
├── video2.txt                 # Анализ для video2.mp4
└── temporary/                 # Промежуточные файлы (создается автоматически)
    ├── video1_chunk_001.mp4   # Куски видео
    ├── video1_chunk_001_analysis.txt
    ├── video1_chunk_002.mp4
    └── video1_chunk_002_analysis.txt
```

## Поддерживаемые форматы видео

- **MP4** (рекомендуется)
- **AVI, MOV, MKV** 
- **WMV, FLV, WebM, M4V**

## Преимущества батчевой обработки

- **Массовый анализ**: Обрабатывайте множество видео одной командой
- **Устойчивость**: Если один видеофайл не удается обработать, остальные продолжают обрабатываться
- **Прогресс**: Подробная статистика по каждому видео и общий прогресс
- **Организованность**: Все видео в одной папке, все результаты в корне проекта
- **Автоматизация**: Просто добавьте новые видео в папку и запустите скрипт

## Конфигурация

Все настройки хранятся в файле `config.env`:

```bash
# Google Cloud configuration
GOOGLE_CLOUD_PROJECT_ID=your-project-id-here  # ОБЯЗАТЕЛЬНО измените!
VERTEX_AI_LOCATION=global                     # Обычно не меняется

# Gemini model configuration  
GEMINI_MODEL_NAME=gemini-2.5-pro             # Можно использовать другие модели

# Video processing configuration
CHUNK_DURATION_MINUTES=10                    # Длительность кусков в минутах
VIDEO_INPUT_DIRECTORY=video                  # Папка с входными видеофайлами
```

### Как получить Google Cloud Project ID

1. **Установите Google Cloud SDK** ([инструкции](https://cloud.google.com/sdk/docs/install))
2. **Откройте [Google Cloud Console](https://console.cloud.google.com/)**
3. **Выберите или создайте проект** в верхней части страницы
4. **Скопируйте Project ID** (не Project Name!) - это строка вида `my-project-123456`
5. **Включите Vertex AI API**:
   - Перейдите в "APIs & Services" → "Library" 
   - Найдите "Vertex AI API" и включите его
6. **Настройте аутентификацию**:
   ```bash
   gcloud auth application-default login
   gcloud config set project YOUR_PROJECT_ID
   ```

### Создание config.env файла

```bash
# 1. Скопируйте шаблон
cp config.env.example config.env

# 2. Откройте в любом редакторе
nano config.env
# или
code config.env

# 3. Замените your-google-cloud-project-id на ваш реальный Project ID
# Пример:
# GOOGLE_CLOUD_PROJECT_ID=my-video-analyzer-project
```

### Безопасность

- Файл `config.env` содержит конфиденциальные данные и **исключен из git**
- Используйте `config.env.example` как шаблон
- Никогда не коммитьте реальные ключи и ID проектов
- Если случайно закоммитили - немедленно пересоздайте проект/ключи

## Обработка ошибок API

Скрипт автоматически обрабатывает ошибки превышения лимитов API (429):
- **Экспоненциальные задержки**: 2, 4, 8, 16, 32 секунды
- **Jitter**: Случайные отклонения для избежания одновременных запросов
- **До 5 попыток** для каждого запроса
- **Сохранение прогресса**: Промежуточные результаты не теряются

## Устранение неполадок

### Частые ошибки при настройке:

**"gcloud command not found" / "Google Cloud SDK not installed"**
- Установите Google Cloud SDK: [https://cloud.google.com/sdk/docs/install](https://cloud.google.com/sdk/docs/install)
- После установки перезапустите терминал
- Проверьте установку: `gcloud --version`

**"Google Cloud Project ID must be provided"**
- Проверьте, что файл `config.env` создан и содержит правильный `GOOGLE_CLOUD_PROJECT_ID`
- Убедитесь, что используете Project ID, а не Project Name

**"Resource exhausted (429)"** 
- Превышены лимиты API. Скрипт автоматически сделает паузу и повторит запрос
- Если ошибка повторяется - проверьте квоты в Google Cloud Console

**"Permission denied / Authentication error"**
- Убедитесь, что Google Cloud SDK установлен: `gcloud --version`
- Выполните аутентификацию: `gcloud auth application-default login`
- Убедитесь, что Vertex AI API включен в вашем проекте
- Проверьте, что используете правильный проект: `gcloud config get-value project`

**"ffmpeg not found"**
- macOS: `brew install ffmpeg`
- Ubuntu: `sudo apt install ffmpeg`
- Windows: скачайте с [ffmpeg.org](https://ffmpeg.org/download.html)

**"No video files found in 'video/' directory!"**
- Убедитесь, что папка `video/` содержит видеофайлы
- Проверьте формат файлов (поддерживаются: MP4, AVI, MOV, MKV, WMV, FLV, WebM, M4V)
- Файлы должны быть непосредственно в папке `video/`, не в подпапках

## Требования

- **Python 3.8+**
- **Google Cloud SDK** ([инструкции по установке](https://cloud.google.com/sdk/docs/install))
- **Google Cloud проект** с включенным Vertex AI API
- **ffmpeg** установлен в системе
- **Видео файлы** в поддерживаемом формате (MP4, AVI, MOV и др.)
- **Настроенная аутентификация** Google Cloud (`gcloud auth application-default login`)
